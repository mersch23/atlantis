version: 3
automerge: false
delete_source_branch_on_merge: true

projects:
  - name: my-infra
    dir: .
    terraform_version: v1.9.8
    autoplan:
      when_modified:
        - "**/*.tf"
        - "**/*.tfvars"
      enabled: true
    apply_requirements:
      - approved
